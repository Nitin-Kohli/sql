--CREATE SCHEMA
--S2

CREATE TABLE S2.EMP
(EMPID INT,DEPID INT)
CREATE TABLE S2.DEP
(DEPID INT,DEPNAME VARCHAR(MAX))

INSERT INTO S2.EMP VALUES(1,1),(2,1),(3,2)
INSERT INTO S2.DEP VALUES(1,'DEV'),(2,'SUPPORT')
SELECT * FROM S2.DEP

SELECT EMP.EMPID,DEP.DEPNAME
FROM S2.EMP
LEFT JOIN 
S2.DEP ON EMP.DEPID = DEP.DEPID AND DEP.DEPID =1

SELECT EMP.EMPID,DEP.DEPNAME
FROM S2.EMP
LEFT JOIN 
S2.DEP ON EMP.DEPID = DEP.DEPID 
WHERE DEP.DEPID = 1
